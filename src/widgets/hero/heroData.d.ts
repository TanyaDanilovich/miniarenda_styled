import { type ImageProps } from '../../shared/types/common.types';
type HeroSlideData = {
    mainText: string;
    additionalText: string;
    image: ImageProps;
};
export declare const heroData: HeroSlideData[];
export {};
